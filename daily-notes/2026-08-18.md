# Day 6 — $(date +%F)

## Topics covered
- Parentheses drill: reference vs. call, across functions, methods, and classes
- Debugged two independent stack/queue bugs (is_empty missing parens, dequeue extra arg)
- Researched deque vs list.pop(0) — amortized O(1) vs O(n) shifting cost
- Resolved a real git divergent-branch situation (merge, not just theory)
- File I/O: write/append/read modes, why "with" replaces manual .close()

## Key wins
- Diagnosed the ShoeRack is_empty bug independently, applying the parentheses drill immediately in a real situation
- Correctly reasoned through append-mode string concatenation, including catching the space living in the string itself
- Handled a genuine git merge conflict situation calmly, following steps correctly
- Caught own redundant/broken file.close references without prompting on the logic (just needed the syntax typo pointed out)

## Bugs fixed
- ShoeRack.pop(): self.is_empty missing parentheses (always truthy)
- BookQueue.dequeue(0): extra unnecessary argument passed to a no-arg method
- File I/O exercise: redundant file.close (no parens, and unnecessary given "with"), and a stray "ith" typo instead of "with"

## Next session
- Error handling in depth: multiple except blocks, finally, custom exceptions
- Modules, packages, virtual environments — closes out Month 1 entirely
