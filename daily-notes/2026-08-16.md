# Day 5 — 2026-08-16

## Topics covered
- Instance vs class vs staticmethod (self/cls/no automatic param) — recap and consolidation
- __new__ vs __init__ — object allocation vs initialization, which runs first and why
- Inheritance deep dive: subclassing, method overriding, super(), the classic "override without super() breaks parent behavior" bug
- Multiple inheritance and MRO (__mro__), how Python resolves which parent's method runs
- Operator overloading: __add__ for combining two custom objects
- Full dunder methods reference (creation, display, arithmetic)
- Decorators from scratch: wrapping functions, the @ shortcut, capturing and transforming a return value
- Encapsulation: single vs double underscore convention, and why convention alone doesn't enforce anything
- @property and @x.setter: real enforced validation vs a polite naming convention

## Key wins
- Correctly diagnosed a reference-vs-call bug (func.upper missing parentheses) fast, independently
- Correctly reasoned that underscore-only "protection" has zero actual enforcement, before being told
- Clearly articulated the difference between naming convention and @property-based enforcement, unprompted

## Bugs fixed
- shout() decorator: called .upper() on the function reference itself instead of its returned string
- shout() decorator (round 2): called .upper without parentheses — reference instead of execution

## Next session
- File I/O: reading/writing .txt, .csv, .json
- Error handling in depth (multiple except, finally, custom exceptions)
- Modules, packages, virtual environments
