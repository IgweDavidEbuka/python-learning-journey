# Day 7 — $(date +%F)

## Topics covered
- Error handling rebuilt from first principles: try/except, why except only catches its named error type
- Multiple except blocks, order of evaluation, only one ever fires
- finally: runs unconditionally regardless of success/failure
- Real debugging: office assessment logic bug (or vs and in range checks) and a silent string-mismatch bug (missing underscore)
- Custom exceptions: class MyError(Exception), raise, except ... as e
- Exception inheritance and except matching — traced 4 distinct scenarios (sibling, child catches via parent, exact match, parent raised but child-named except fails to catch)

## Key wins
- Correctly traced multiple realistic try/except/finally scenarios (success, ValueError, ZeroDivisionError) unprompted, matching output exactly
- Diagnosed a genuine logic bug in real assessment code (or vs and) through deliberate manual tracing with a chosen test value, not guesswork
- Diagnosed a silent string-equality bug (missing underscore) independently after being pointed toward repr()
- Self-corrected an exception-inheritance misconception (thought InsufficientFundsError was a subtype of ValueError) once walked through the actual class tree
- Correctly reasoned through all 4 inheritance/except-matching scenarios unaided after one worked example
- Caught own test case flaw (testing set_age(0) instead of a genuinely negative number) and fixed it independently

## Bugs fixed
- Assessment: Age >= 5 or Age <= 12 (and same pattern in next branch) — should have been `and`, since `or` was true for nearly all inputs
- Assessment: "Day type" vs "Day_type" — silent string mismatch caught via repr()
- try/except exercise: initial finally message read oddly on the success path (noted as design consideration, not a bug)
- Custom exception exercise: test case (0) didn't actually trigger the negative-age condition being tested

## Next session
- Modules, packages, virtual environments (venv) — closes out Month 1 entirely
