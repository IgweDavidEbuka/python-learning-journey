# Day 2 — August 4, 2026

## Topics covered
- Functions from the ground up: def vs calling, parentheses as "execute now"
- Parameters (single and multiple), positional vs keyword arguments
- return vs print — what each actually does, and why they're not interchangeable
- (Extra, work-driven) recursion & call stack, sys.getrecursionlimit()
- (Extra) list/string slicing with step values, including negative step reversal
- (Extra) sorted() vs .sort() — return value, memory trade-off
- (Extra) lambda used as sort key on lists of dicts and dict.items()

## Key wins
- Correctly explained why a bare function reference (no parentheses) doesn't execute
- Self-corrected two separate return-related bugs without needing the concept re-explained
- Independently reasoned that keyword arguments work regardless of def-line order
- Traced call stack / recursion depth conceptually before running any code

## Bugs fixed
- return with nothing attached to it (calculation and return written on separate, disconnected lines)
- Positional argument math error caught via manual trace, not just told the answer
- Slice indexing confusion (assumed step corresponded to "even numbers" instead of raw index position)

## Next session
- Recursion, hands-on: writing and tracing simple recursive functions
- OOP introduction (classes) — next major fundamentals topic
