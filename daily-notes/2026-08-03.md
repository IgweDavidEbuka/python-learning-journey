# Day 1 — August 3, 2026

## Topics covered
- for vs while: when to use which, and why
- Manual counters (i = 0, i += 1) — traced by hand across several variations
- Nested loops — row/column mental model, predicting order and count
- List comprehensions — built from a loop first, then written directly
- map(), filter(), sorted() — including key and reverse arguments

## Key wins
- Reasoned for vs while from first principles, unprompted
- Traced loop counters by hand correctly across 3 different variations
- Predicted nested loop output before writing any code
- Caught and fixed 3 bugs of the same underlying shape (reaching for the whole container instead of the specific item/function needed) across different contexts

## Bugs fixed
- print(list.append(...)) — mixed up "building" a list with "showing" it
- letters.upper() vs str.upper — called a method on the container instead of handing map() the unattached method
- sorted() with reverse nested inside key instead of as its own argument

## Next session
- Functions from the ground up: def, return vs print, parameters
