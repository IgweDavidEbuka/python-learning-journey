# Day 3 — August 5, 2026

## Topics covered
- Recursion, hands-on: base case vs recursive case
- Tracing recursive calls "going in" (countdown) and "unwinding" (count_up)
- Call stack LIFO behavior, connected directly to earlier recursion theory

## Key wins
- Correctly traced countdown(n) including the n=0 edge case unprompted
- Persisted through 3 attempts at count_up without giving up or guessing blindly
- Diagnosed a separate bug (missing function call) independently, correctly distinguishing it from a recursion logic error
- After getting outside input, came back and self-corrected the precise wording of the unwind explanation rather than accepting it as-is

## Bugs fixed
- Base case condition checked the wrong thing (caught the starting value instead of the true "done" state) — recurred across 3 attempts before landing
- Recursive call placed in the wrong branch relative to the base case
- Forgot to actually call the function after defining it (separate from the recursion logic itself)

## Next session
- Revisit recursion — one more pattern (recursive function that returns/builds a value, not just prints)
- OOP introduction (classes) — still pending, pushed from today
